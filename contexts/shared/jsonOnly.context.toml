[[sections]]
id = "output.json-only"
kind = "output-contract"
severity = "must"
enforcement = "prompt"
optimizable = false
text = "JSON のみを返してください。"

[sections.translations."en-US"]
text = "Return JSON only."
